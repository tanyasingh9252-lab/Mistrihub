name: mistri_hub
description: Professional construction workers and builders directory platform.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

  dependencies:
    flutter:
        sdk: flutter
          cupertino_icons: ^1.0.5
            firebase_core: ^2.24.0
              firebase_auth: ^4.15.0
                cloud_firestore: ^4.13.0
                  firebase_storage: ^11.5.0
                    firebase_messaging: ^14.7.5
                      provider: ^6.1.1
                        razorpay_flutter: ^1.3.5
                          url_launcher: ^6.2.2
                            cached_network_image: ^3.3.0
                              intl: ^0.18.1

                              flutter:
                                uses-material-design: truename: mistri_hub
description: Professional construction workers and builders directory platform.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  firebase_core: ^2.24.0
  firebase_auth: ^4.15.0
  cloud_firestore: ^4.13.0
  firebase_storage: ^11.5.0
  firebase_messaging: ^14.7.5
  provider: ^6.1.1
  razorpay_flutter: ^1.3.5
  url_launcher: ^6.2.2
  cached_network_image: ^3.